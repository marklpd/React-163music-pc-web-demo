import styled from "styled-components";

export const LyricsShowWrapper = styled.div`
  position: absolute;
  left: 50%;
  transform:translateX(-50%);
  bottom: 50px;
  height: 40px;
  line-height: 40px;
  color: rgba(255,0,0,.7);
  font-size: 20px;

  background-color: rgba(0,0,0,0);
`